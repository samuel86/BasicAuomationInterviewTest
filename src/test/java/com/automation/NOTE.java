package com.automation;

public class NOTE {
    /**
     * FIRST thing to do
     * 1. Add some dependencies into the pom.xml file
     *   a.WebDriverManager
     *   b.Selenium Java
     *  These two dependencies will help us to automate
     * 2. Set up a Driver
     * 3. Then we create ChromeDriver
     */
}
